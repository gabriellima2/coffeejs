import styled from "styled-components";

import colors from "../../assets/colors";

export const LogoStyle = styled.a`
    display: flex;
    align-items: center;
    font-size: 1.4em;
    letter-spacing: 5px;
    color: ${colors.logo};
`;
